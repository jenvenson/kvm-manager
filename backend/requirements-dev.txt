# Test-only dependencies (the runtime deps live in requirements.txt).
# libvirt-python is intentionally omitted: the suite stubs it out so tests run
# without a libvirt host.
-r requirements.txt
pytest==8.2.2
httpx==0.27.0
